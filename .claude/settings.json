{
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "shell": "bash",
            "command": "{ cd \"C:/Users/Administrator/Desktop/www-master\" && git add -A && { git diff --cached --quiet || git commit -q -m \"auto-save: $(date '+%Y-%m-%d %H:%M:%S')\" --no-verify; } && git push -q origin master ; } 2>/dev/null || true",
            "statusMessage": "自动保存并推送到 GitHub…",
            "timeout": 60
          }
        ]
      }
    ]
  }
}
